<%= form_for @task do |t| %>
  <%= t.label :title, 'Title' %>
  <%= t.text_field :title %>
  <%= t.label :details, 'Details' %>
  <%= t.text_field :details %>
  <%= t.label :completed, 'Completed?' %>
  <%= t.check_box :completed %>
  <%= t.submit %>
<% end %>
